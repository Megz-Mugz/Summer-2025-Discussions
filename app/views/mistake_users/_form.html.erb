<%= form_with(model: mistake_user) do |form| %>
  <% if mistake_user.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(mistake_user.errors.count, "error") %> prohibited this mistake_user from being saved:</h2>

      <ul>
        <% mistake_user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
